# AP Firmware Ghidra Scripts

Java Ghidra scripts used to inspect the AP firmware command parser, SPI flash helpers, timeout/timer path, and patch sites.

The scripts are included as source-only research artifacts. Generated Ghidra projects, dumps, caches, and firmware blobs are intentionally excluded.
