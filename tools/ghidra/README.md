# Ghidra Scripts

These scripts were used during offline reverse engineering of GIGABYTE/AORUS LCD firmware and updater binaries.

Only script source files are kept here. Ghidra project databases, imported vendor binaries, DLLs, EXEs, and firmware blobs are intentionally not committed.
